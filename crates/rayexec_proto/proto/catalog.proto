syntax = "proto3";

package rayexec.catalog;

import "expr.proto";

message AttachInfo {
    string                             datasource = 1;
    map<string, expr.OwnedScalarValue> options    = 2;
}
